=== TABLOLAR (7 adet) ===

--- BannedDevices ---
  id (int, nullable=NO)
  device_id (varchar, nullable=NO)
  banned_at (datetime, nullable=YES)

--- Kategoriler ---
  id (int, nullable=NO)
  kategori_adi (nvarchar, nullable=NO)
  aktif_mi (bit, nullable=YES)

--- Kullanicilar ---
  id (int, nullable=NO)
  kullanici_adi (nvarchar, nullable=NO)
  sifre_hash (nvarchar, nullable=NO)
  rol (nvarchar, nullable=NO)

--- Masalar ---
  id (int, nullable=NO)
  masa_no (nvarchar, nullable=NO)
  qr_kodu (nvarchar, nullable=NO)
  durum (nvarchar, nullable=YES)

--- SiparisDetaylari ---
  id (int, nullable=NO)
  siparis_id (int, nullable=NO)
  urun_id (int, nullable=NO)
  adet (int, nullable=NO)
  birim_fiyat (decimal, nullable=NO)
  urun_notu (nvarchar, nullable=YES)
  ara_toplam (decimal, nullable=NO)

--- Siparisler ---
  id (int, nullable=NO)
  masa_id (int, nullable=NO)
  siparis_kodu (nvarchar, nullable=NO)
  toplam_tutar (decimal, nullable=NO)
  odeme_durumu (nvarchar, nullable=YES)
  siparis_durumu (nvarchar, nullable=YES)
  olusturma_tarihi (datetime, nullable=YES)
  garson_adi (nvarchar, nullable=YES)
  device_id (varchar, nullable=YES)

--- Urunler ---
  id (int, nullable=NO)
  kategori_id (int, nullable=NO)
  urun_adi (nvarchar, nullable=NO)
  aciklama (nvarchar, nullable=YES)
  fiyat (decimal, nullable=NO)
  gorsel_url (nvarchar, nullable=YES)
  stok_miktari (int, nullable=YES)
  aktif_mi (bit, nullable=YES)
